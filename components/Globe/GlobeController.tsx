'use client';

// GlobeController is a thin component that drives the globe transforms
// from gesture state. The actual logic lives in useGlobeControls.
// This component is intentionally empty — it only wires up the store.
// The GlobeModel reads from the same hook.
export default function GlobeController() {
  return null;
}
